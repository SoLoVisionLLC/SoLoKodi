# SoLoKids TV Build Overrides

Drop replacement files here using the same relative paths found inside the
foundation build ZIP. The build packager applies these files after text
rebranding, then generates the SoLoKids TV home menu shortcut XML.
